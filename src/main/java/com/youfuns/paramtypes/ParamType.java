package com.youfuns.paramtypes;

public enum ParamType {
    // DATE was removed as it is better to use actual date storage classes
    NAME, USERNAME, EMAIL, PHONE, PASSWORD, HASH, FILEPATH, JWT, ADDRESS, CONTACT_METHOD, SKU, GENERIC
}